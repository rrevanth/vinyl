import { observer } from '@legendapp/state/react'
import { UICustomizationSettings } from '@/src/features/settings/components/organisms/UICustomizationSettings'

const UICustomizationScreen = observer(() => {
  return <UICustomizationSettings />
})

export default UICustomizationScreen
