import { observer } from '@legendapp/state/react'
import { PlaybackSettings } from '@/src/features/settings/components/organisms/PlaybackSettings'

const PlaybackScreen = observer(() => {
  return <PlaybackSettings />
})

export default PlaybackScreen
