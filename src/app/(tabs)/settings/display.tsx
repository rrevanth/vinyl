import { observer } from '@legendapp/state/react'
import { DisplaySettings } from '@/src/features/settings/components/organisms/DisplaySettings'

const DisplayScreen = observer(() => {
  return <DisplaySettings />
})

export default DisplayScreen
