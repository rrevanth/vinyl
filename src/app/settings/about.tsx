import { observer } from '@legendapp/state/react'
import { AboutSettings } from '@/src/features/settings/components/organisms/AboutSettings'

const AboutScreen = observer(() => {
  return <AboutSettings />
})

export default AboutScreen
